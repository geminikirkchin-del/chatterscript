services:
  chatterscript:
    build:
      context: .
      dockerfile: Dockerfile.cu130
      args:
        RUNTIME: nvidia
    image: chatterscript:cu130
    container_name: chatterscript-cu130
    ports:
      - "8004:8004"
    volumes:
      - ./model_cache:/app/model_cache
      - ./reference_audio:/app/reference_audio
      - ./outputs:/app/outputs
      - ./voices:/app/voices
      - ./logs:/app/logs
      - ./config.yaml:/app/config.yaml
      - ~/.cache/huggingface:/app/hf_cache
    env_file: .env
    environment:
      - HF_TOKEN=${HF_TOKEN}
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
